def top_k_frequent(nums, k):
    #empty object to keep track of how many times each integer appears in the array
    #iterate over array. check for current integer. if it's in the objet, increment it's number. if it's not, add it.
    #